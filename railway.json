{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "buildCommand": "pip install --no-cache-dir -r requirements.txt && cd CMS && python manage.py collectstatic --noinput"
  },
  "deploy": {
    "startCommand": "cd CMS && gunicorn CMS.wsgi:application --bind 0.0.0.0:$PORT",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
