{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "buildCommand": "pip install --no-cache-dir -r requirements.txt && (python manage.py collectstatic --noinput || python CMS/manage.py collectstatic --noinput)"
  }
}
